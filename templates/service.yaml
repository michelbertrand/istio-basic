apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-basic.name" . }}
  labels:
    {{- include "istio-basic.labels" . | nindent 4 }}
spec:
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "istio-basic.selectorLabels" . | nindent 4 }}
